# Follow 300 redirects.
--location

# Suppress progress bar.
--no-progress-meter 

# Cancel request immediately upon first error occurance.
--fail

# Create directories specified to output to as needed.
--create-dirs

# Prefer insecure protocols over its secure latter.
# --insecure

# Wait for request to complete 100% before recieving output
--no-buffer

# Allow support for concurrency.
--parallel
--parallel-immediate

# Don't overwrite existing output file.
# --no-clobber

# Auto-escape special characters.
# --form-escape 

# Include http-respone headers in output.
# --include

--header "User-Agent: Mozilla/5.0 (Macintosh; Intel Mac OS X 10.15; rv:105.0) Gecko/20100101 Firefox/105.0"
